-- in the local script

-- outside the damage applier

local leaderstats = player:FindFirstChild("leaderstats")
local add_damage = game.ServerScriptService:FindFirstChild("add_damage")

-- inside the damage applier

add_damage:Fire(damage, leaderstats)

-- in game.ServerScriptService.add_damage

local add_damage = Instance.new("BindableEvent")
add_damage.Name = "add_damage"
add_damage.Parent = game.ServerScriptService


local function damage_deal(damage, leaderstats)
	local player_damage = leaderstats:FindFirstChild("Damage")
	player_damage.Value += damage
	event:Disconnect()
end

local event = add_damage.Event:Connect(damage_dealer)



